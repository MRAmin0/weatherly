# Weatherly

اپلیکیشن Weatherly برای مشاهده وضعیت آب‌وهوا با جستجوی سریع شهر، پشتیبانی کامل از زبان فارسی و تم تاریک/روشن.<br> 
نسخهٔ آنلاین از طریق [این لینک](https://weatherylapp.netlify.app/) در دسترس است.

## 📸 نمای کلی

| صفحه خانه                  | نتایج جستجو                  | تنظیمات                        |
| -------------------------- | ---------------------------- | ------------------------------ |
| ![Home](Screenshots/2.png) | ![Search](Screenshots/2.png) | ![Settings](Screenshots/3.png) |

## ✨ امکانات

- جستجوی هوشمند شهرها با امتیازدهی به نتایج فارسی و لیست پیشنهادها
- ذخیره و نمایش جستجوهای اخیر برای دسترسی سریع
- نمایش وضعیت فعلی، پیش‌بینی ۵ روزه و دمای ساعتی (با امکان غیرفعال‌سازی)
- محاسبه و نمایش شاخص کیفیت هوا (AQI) با برچسب و رنگ‌بندی فارسی
- ذخیره تنظیمات کاربر (تم، واحد دما، شهر پیش‌فرض) در `SharedPreferences`
- ساختار ماژولار: سرویس API مجزا، مدل‌های مشترک و یوتیلیتی‌های فرمت/شهر

## 🗂 ساختار پوشه‌های مهم

```
lib/
├── main.dart                 // بوت‌استرپ و تعریف تم‌ها
├── config_reader.dart        // بارگذاری کلیدها از keys.json
├── weather_store.dart        // منطق حالت و تعامل با سرویس‌ها
├── models/                   // مدل‌ها (WeatherType، HourlyForecastResponse)
├── services/                 // WeatherApiService برای فراخوانی‌های HTTP
├── utils/
│   ├── city_utils.dart       // نرمال‌سازی و امتیازدهی شهرها
│   └── weather_formatters.dart // فرمت اعداد، آیکون‌ها، ترجمه وضعیت
├── screens/                  // صفحات اصلی (خانه، تنظیمات، درباره و ...)
└── widgets/                  // ویجت‌های سفارشی (آیتم‌های پیش‌بینی و ...)
```

## ⚙️ راه‌اندازی

1. **پیش‌نیازها**

   ```bash
   flutter --version  # Flutter 3.16 یا بالاتر پیشنهاد می‌شود
   ```

2. **نصب وابستگی‌ها**

   ```bash
   flutter pub get
   ```

3. **تنظیم کلید API (ضروری)**

   - فایل `keys.json` را در ریشه پروژه بسازید (در کنار `pubspec.yaml`).
   - محتوای زیر را قرار دهید:
     ```json
     {
       "openweathermap_api_key": "YOUR_OPENWEATHERMAP_KEY"
     }
     ```
   - مطمئن شوید `pubspec.yaml` شامل Asset زیر است:
     ```yaml
     flutter:
       assets:
         - keys.json
     ```

4. **اجرای پروژه**
   ```bash
   flutter run
   ```

## 🧪 توسعه و تست

- اجرای تحلیل کد: `flutter analyze`
- اجرای تست‌های ویجت موجود: `flutter test`
- برای دیباگ روی وب از Chrome: `flutter run -d chrome`

## 📮 مشارکت

پیشنهاد، باگ یا فیچر جدید داری؟ در GitHub Issue ثبت کن یا Pull Request بفرست. نام‌گذاری شاخه‌ها و توضیح کامل تغییرات کمک بزرگی است.

## 🙏 با تشکر

- داده‌های هواشناسی: [OpenWeatherMap](https://openweathermap.org/api)
- مدیریت حالت: بستهٔ عالی [provider](https://pub.dev/packages/provider)

---

ساخته شده با ❤️ توسط Amin. اگر از پروژه خوشت اومد حتماً ⭐️ بده!
